# Flask and extensions
Flask==3.0.0
Flask-CORS==4.0.0
python-dotenv==1.0.0

# Supabase
supabase>=2.10.0

# HTTP requests
requests==2.31.0
httpx>=0.27.0

# Data validation
pydantic>=2.0.0

# Utilities
python-dateutil==2.8.2

# Google Maps (optional, for geocoding)
googlemaps==4.10.0

# Authentication
bcrypt>=4.0.0
PyJWT>=2.0.0

# Payment Gateway
razorpay>=1.4.0

# Production server
gunicorn==21.2.0

# Email API (works on Render - uses HTTPS instead of blocked SMTP ports)
resend>=2.0.0
sendgrid>=6.9.0
